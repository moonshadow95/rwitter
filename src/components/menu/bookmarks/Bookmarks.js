import React from "react";

const Bookmarks = (props) => (
  <div>
    <span>You haven’t added any Tweets to your Bookmarks yet</span>
    <small>When you do, they’ll show up here.</small>
  </div>
);

export default Bookmarks;
